<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Показать мой профиль", current_user %>
</span>
<span>
  <%= current_user.microposts.count %> <%= Russian::pluralize(current_user.microposts.count, "сообщение", "сообщения", "сообщений") %>
</span>